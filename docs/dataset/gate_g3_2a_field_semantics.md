# Gate G3.2-A — Semántica de campos curatoriales

## Estado

APROBADO

## Evidencia

### MET

- `classification` tiene una única categoría: `Textil`.
- `object_type` contiene 23 categorías.
- Igualdad registro por registro: 0 %.

### CMA

- `classification` contiene 4 categorías.
- `object_type` contiene las mismas 4 categorías.
- Igualdad registro por registro: 100 %.

## Decisiones

- `classification` no se utilizará como ground truth discriminativo.
- `object_type` no se utilizará como ground truth interinstitucional.
- `object_type` se conservará para análisis cualitativo y análisis específico
  por fuente.
- La armonización global continuará con `culture` y `period`.
- No se inferirán tipos de objeto CMA a partir de imágenes o títulos en esta
  etapa.

## Pruebas

29 pruebas aprobadas.

## Decisión final

Se autoriza la construcción del inventario de cultura y periodo.